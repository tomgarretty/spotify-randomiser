html
body
  if isAuthenticated
    each playlist in playlists
      form(action='/api/shuffle?id=' + playlist.id, method='POST')
        input(type='submit' value='Shuffle ' + playlist.name)
  else
    a(class='button' href='/api/login') Authenticate with Spotify
